import React from 'react'
import PropTypes from 'prop-types'

import Dialog from './Dialog'

import './GameWonDialog.css'

const GameWonDialog = (props) => (
	<Dialog title="Jeu Terminé !">
		c'est gagné !
	</Dialog>
)

export default GameWonDialog